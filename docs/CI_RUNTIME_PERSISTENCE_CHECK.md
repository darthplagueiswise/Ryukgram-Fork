# Runtime persistence CI verification

This branch is intentionally identical to `dogfood` in compiled sources. This Markdown file only triggers the pull-request workflow so the SDK 26.5 rootless/rootful build and Mach-O validation can be inspected through the GitHub connector.
