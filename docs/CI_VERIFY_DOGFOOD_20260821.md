# Dogfood CI verification

This branch exists only to trigger the pull-request workflow against the exact `dogfood` source tree at `40345770523200c4eb2ee14d4802950b8111aade`. The file has no build/runtime effect.
