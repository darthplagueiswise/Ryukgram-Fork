#import <UIKit/UIKit.h>

// Settings sub-page that mirrors the gallery's storage / clear screen so the
// log feature follows the same UX everywhere — total counts, on-disk size,
// and per-account / global wipe controls.
@interface SCIDeletedMessagesStorageViewController : UIViewController
@end
