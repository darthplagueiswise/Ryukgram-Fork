The following experiments2 files were intentionally removed from compilation:
- SCILiquidGlassForce.x
- SCIStoryTrayForce.x
- SCIStatusBarOldSchoolForce.x
- SCIWordmarkForce.x

Reason: they installed persisted getter overrides from %ctor. That reintroduced
launch-path execution after the 4207 baseline and contradicts the persistent-no-launch
rules in CLAUDE.md / THEOS.md. The UI now applies these presets explicitly through
SCIBulkGatingPresets / SCIGatingCatalog when the user toggles or selects a mode.
