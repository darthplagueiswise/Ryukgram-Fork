// DM disappearing-media overlay buttons (action / eye / audio). Positions + slot
// grid come from SCIOverlayButtonLayout; this only declares the button set.

#import "../../UI/DragLayout/SCIOverlayButtonLayout.h"

extern NSString *const SCIDMBtnAction;
extern NSString *const SCIDMBtnEye;
extern NSString *const SCIDMBtnAudio;

@interface SCIDMButtonLayout : SCIOverlayButtonLayout
@end
