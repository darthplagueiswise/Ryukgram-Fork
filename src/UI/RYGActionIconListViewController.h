// RYGActionIconList — entry screen for action button icons.
// Lists the shared default plus one row per overridable button; each row
// opens the grid picker (global or per-source).

#import <UIKit/UIKit.h>

NS_ASSUME_NONNULL_BEGIN

@interface RYGActionIconListViewController : UITableViewController
@end

NS_ASSUME_NONNULL_END
